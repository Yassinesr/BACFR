config文件夹中是每个模型的配置信息，其中BACFR.yaml是我的模型。
其他的配置文件是UACANET项目中自带的。只有BACFR.yaml是我基于UACANET项目新增的模型。

The "config" folder contains the configuration information for each model. Among them, "BACFR.yaml" is my model.
The other configuration files are provided by the UACANET project itself. Only "BACFR.yaml" is a new model I added based on the UACANET project.
